# Docker Compose Configuration
COMPOSE_PROJECT_NAME=shiny-fiesta

# ==============================================
# Application Secrets - CHANGE THESE VALUES!
# ==============================================
# Database Secrets
POSTGRES_USER=postgres
POSTGRES_PASSWORD=change_this_password

# pgAdmin Secrets
PGADMIN_DEFAULT_EMAIL=admin@example.com
PGADMIN_DEFAULT_PASSWORD=change_this_password

# Security Secrets
SECRET_KEY_BASE=generate_a_secret_key_base_here
LIVEBOOK_TOKEN=change_this_token

# ==============================================
# Configuration Values - Safe to commit
# ==============================================
# Database Configuration
POSTGRES_DB=exins_dev

# Service Ports
PGADMIN_LISTEN_PORT=5050
LIVEBOOK_PORT=8080
PORT=4000

# Application Settings
PHX_HOST=localhost